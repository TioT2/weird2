[package]
name = "weird"
version = "0.1.0"
edition = "2024"

[profile.dev]
opt-level = 0

[profile.release]
lto = "fat"

[dependencies]
sdl2 = "0.38.0"
thiserror = "2.0.18"
zerocopy = { version = "0.8.48", features = ["derive"] }
# debug = "line-tables-only"